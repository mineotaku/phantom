package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Phantom Premium Light Sage & Warm Cream Palette (Material You Style)
val PhantomBg = Color(0xFFF6F8F2)           // Beautiful warm cream with slight sage tint
val PhantomSurface = Color(0xFFFFFFFF)      // Crisp clean white for containers
val PhantomSurfaceVariant = Color(0xFFECEFE5)// Soft warm gray-green
val PhantomPrimary = Color(0xFFD5E897)      // Vibrant warm lime-green (like floating action buttons)
val PhantomSecondary = Color(0xFF43493E)    // Sophisticated dark forest charcoal
val PhantomTertiary = Color(0xFFA1AF97)     // Subdued sage green
val PhantomError = Color(0xFFBA1A1A)        // Soft critical red
val PhantomTextPrimary = Color(0xFF1A1C18)  // Deep professional charcoal
val PhantomTextSecondary = Color(0xFF5D6156)// Balanced mid-ground gray-green
val PhantomBorder = Color(0xFFE1E4D5)       // Ultra-thin soft border color
val PhantomCardBg = Color(0xFFF0F2EB)       // Soft card background
val PhantomTerminalBg = Color(0xFFECEFE5)   // Light console display
val PhantomSelectedChat = Color(0xFFE2EED0) // Subtle highlighted item green
val PhantomSentBubble = Color(0xFFD5E897)   // Light-lime sent bubble
val PhantomReceivedBubble = Color(0xFFECEFE5)// Soft grey-green received bubble
